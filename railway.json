{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm install --legacy-peer-deps --include=dev && cd packages/types && npm run build && cd ../../apps/server && npm run build"
    },
    "deploy": {
        "startCommand": "cd apps/server && npm run start:railway",
        "numReplicas": 1,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}