{
  "name": "@beacon/mobile",
  "version": "2.3.0",
  "private": true,
  "scripts": {
    "dev": "cap sync && cap open android",
    "build": "cap sync && cap copy android",
    "build:apk": "npm run build && cd android && ./gradlew assembleRelease",
    "build:bundle": "npm run build && cd android && ./gradlew bundleRelease",
    "build:debug": "npm run build && cd android && ./gradlew assembleDebug",
    "sync": "cap sync android",
    "open": "cap open android",
    "clean": "cd android && ./gradlew clean && cd .. && rm -rf node_modules dist",
    "lint": "eslint src --ext ts,tsx"
  },
  "dependencies": {
    "@beacon/types": "file:../../packages/types",
    "@beacon/api-client": "file:../../packages/api-client",
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.3.0"
  }
}