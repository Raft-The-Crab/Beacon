[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["cd apps/server && npm run build"]

[start]
cmd = "cd apps/server && npm run start:railway"