[phases.setup]
nixPkgs = ["nodejs_23", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm install --legacy-peer-deps --include=dev"]

[phases.build]
cmds = ["cd packages/types && npm run build && cd ../../apps/server && npm run build"]

[start]
cmd = "cd apps/server && npm run start:railway"