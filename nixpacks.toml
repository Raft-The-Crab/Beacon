[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm install --legacy-peer-deps --include=dev"]

[phases.build]
cmds = ["NPM_CONFIG_PRODUCTION=false cd apps/server && npm run build"]

[start]
cmd = "cd apps/server && npm run start:railway"